---
import Layout from '../layouts/Layout.astro';
import { playlists } from '../lib/data';
import PlaylistItemCard from '../components/PlaylistItemCard.astro';
import Greetings from '../components/Greetings.svelte';
---

<Layout title="Spotify Astro">
 <div id="playlist-container" class="relative transition duration-1000 bg-green-600">
  <!-- PageHeader -->
  <div class="relative z-10 px-6 pt-10">
    <Greetings />
    <div class="flex flex-wrap mt-6 gap-4">
      {
        playlists.map(playlist => <PlaylistItemCard playlist={playlist}/> )
      }
    </div>
  </div>
  <div class="absolute inset-0 bg-gradient-to-t from-zinc-900 via-zinc-900/80"></div>
 </div>
</Layout>
